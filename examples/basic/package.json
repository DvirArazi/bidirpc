{
  "name": "basic",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n server,client \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "tsx watch server.ts",
    "dev:client": "vite --host 0.0.0.0"
  },
  "dependencies": {
    "@bidirpc/client": "0.0.1",
    "@bidirpc/server": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "concurrently": "^9.0.0",
    "vite": "^7.0.0"
  }
}